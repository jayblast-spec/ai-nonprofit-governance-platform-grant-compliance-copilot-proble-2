'use client';

import { useState } from 'react';

type Output = { title: string; summary: string; score: number; insights: string[]; actions: string[]; upgrade_prompt: string };

export default function AppPage() {
  const [input, setInput] = useState('');
  const [output, setOutput] = useState<Output | null>(null);
  const [loading, setLoading] = useState(false);
  async function generate() { setLoading(true); const response = await fetch('/api/spec', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ input }) }); const data = await response.json(); setOutput(data.output); setLoading(false); }
  return <main className="min-h-screen bg-[#f9f9f6] text-[#0a0a0a]"><nav className="sticky top-0 z-30 border-b border-[#e2e8f0] bg-[#f9f9f6]/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6"><a href="/" className="text-lg font-black tracking-tight text-[#0a0a0a]">AI Nonprofit Governance Platform</a><div className="hidden items-center gap-6 text-sm text-[#787960] md:flex"><a href="/features">Features</a><a href="/how-it-works">How it works</a><a href="/pricing">Pricing</a><a href="/dashboard">Dashboard</a><a href="/#contact" className="rounded bg-[#0a0a0a] px-3 py-2 font-semibold text-[#e4f222]">Start a project</a></div><a href="/#contact" className="md:hidden rounded bg-[#0a0a0a] px-3 py-2 font-semibold text-[#e4f222]">Start</a></div></nav><section className="mx-auto max-w-7xl px-4 py-8 sm:px-6"><div className="grid gap-5 lg:grid-cols-[280px_minmax(0,1fr)_380px]"><aside className="rounded-lg border border-[#e2e8f0] bg-white p-4"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#5d6300]">Workspace</p><h1 className="mt-3 text-2xl font-semibold">AI Nonprofit Governance Platform</h1><div className="mt-6 space-y-2 text-sm text-[#787960]"><Step label="1" text="Paste messy context" /><Step label="2" text="Generate structured output" /><Step label="3" text="Review risks and actions" /><Step label="4" text="Upgrade to save/export" /></div></aside><section className="rounded-lg border border-[#e2e8f0] bg-white p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#5d6300]">Free trial workflow</p><h2 className="mt-2 text-3xl font-semibold">Generate a reviewed result</h2></div><span className="text-xs font-black rounded bg-[#0a0a0a] px-3 py-2 font-semibold text-[#e4f222]">LIVE</span></div><textarea value={input} onChange={(event) => setInput(event.target.value)} placeholder="Paste the real situation, task, customer message, document excerpt, or messy idea..." className="mt-5 min-h-80 w-full rounded-md border border-[#e2e8f0] bg-white p-4 text-sm leading-6 text-[#0a0a0a] outline-none focus:border-current" /><button onClick={generate} disabled={loading || !input.trim()} className="mt-4 min-h-12 w-full font-bold disabled:opacity-50 rounded bg-[#0a0a0a] px-3 py-2 font-semibold text-[#e4f222]">{loading ? 'Generating...' : 'Generate result'}</button></section><aside className="rounded-lg border border-[#e2e8f0] bg-white p-5"><div className="flex items-center justify-between"><h2 className="text-xl font-semibold">Review queue</h2><span className="text-xs font-bold text-emerald-300">Human approval</span></div>{!output ? <p className="mt-4 text-sm leading-6 text-[#787960]">Your score, insights, actions, and upgrade path will appear here after generation.</p> : <div className="mt-5 space-y-4"><div className="rounded-md bg-white p-4"><p className="text-5xl font-semibold text-[#5d6300]">{output.score}%</p><p className="mt-3 text-sm leading-6 text-[#787960]">{output.summary}</p></div><Block title="Insights" items={output.insights} /><Block title="Actions" items={output.actions} /><a href="/pricing" className="block text-center text-sm font-bold rounded bg-[#0a0a0a] px-3 py-2 font-semibold text-[#e4f222]">{output.upgrade_prompt}</a></div>}</aside></div></section></main>;
}
function Step({ label, text }: { label: string; text: string }) { return <div className="flex gap-3 rounded-md bg-white p-3"><span className="flex h-6 w-6 shrink-0 items-center justify-center rounded text-xs font-black rounded bg-[#0a0a0a] px-3 py-2 font-semibold text-[#e4f222]">{label}</span><span>{text}</span></div>; }
function Block({ title, items }: { title: string; items: string[] }) { return <section><h3 className="mb-2 text-xs font-bold uppercase tracking-[0.18em] text-[#5d6300]">{title}</h3><ul className="space-y-2 text-sm text-[#787960]">{items.map((item) => <li key={item} className="rounded-md bg-white p-3">{item}</li>)}</ul></section>; }
